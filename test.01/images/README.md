# 圖片資源目錄

請將您的2D圖示圖片放在此目錄中。

## 格式要求

- 支持格式：`.png`, `.jpg`, `.jpeg`
- 建議使用 `.png` 格式（支持透明背景）
- 圖片大小建議：512x512 或 1024x1024 像素
- 文件大小建議控制在 1MB 以內

## 示例

將圖片命名為 `icon.png` 並放在此目錄，即可在AR場景中使用。

